import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingLeft: 40,
    paddingRight: 40,
    marginBottom: 10,
  },

  label: {
    fontSize: 20,
    color: '#1B262C'
  }
});

export default styles;